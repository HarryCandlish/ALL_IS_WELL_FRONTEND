import React, { Component } from "react";
import philosophyStyles from "../modules/store.module.scss";
import { connect } from "react-redux";

class Store extends Component {
  render() {
    return (
      <div>
        <h1>Store</h1>
      </div>
    );
  }
}

export default Store;
